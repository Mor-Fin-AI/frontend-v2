/** Neutral stat card styling — theme-aware foreground on muted icon background. */
export const STAT_CARD_NEUTRAL = {
  iconBg: "bg-muted",
  iconColor: "text-foreground",
  valueColor: "text-foreground",
} as const;
